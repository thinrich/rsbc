clientSideValidations.validators.local['feb'] = function(element, options) {
  var res;
  if( ( res = neg_month_b( x_func, cellvalue( "month" ) ) instanceof expr ) ) {
    return options.message;
  }
}
